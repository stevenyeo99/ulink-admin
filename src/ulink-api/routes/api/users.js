const express = require('express');
const router = express.Router();

// Example resource route — replace with real controller/service logic.
router.get('/', (req, res) => {
  res.json({ data: [] });
});

module.exports = router;
