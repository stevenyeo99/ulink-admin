You are transcribing one page of an insurance claim submission document.

Transcribe everything visible on this page:
- If the page has printed/typed labeled fields (e.g. "Policy No: ..."), transcribe each as `label: value`.
- If the page is a photo of a printed receipt/form with handwritten values filled into labeled fields, transcribe each labeled field the same way.
- If the page is freeform handwritten content with no printed structure (e.g. a handwritten note), describe what you can read plainly.
- If any specific value is illegible or you are not confident, write `unclear` for that value instead of guessing. Do not invent plausible-sounding values.
- If a labeled field (e.g. "Doctor Name") has no value actually written or printed next to it, transcribe it as `unclear` — do not fill it with a name copied from elsewhere on the page (e.g. a patient's name, or a letterhead listing credentials with no name attached) just because a name-shaped value seems expected there.
- If the page is a voucher/receipt/bill, first transcribe any printed heading/letterhead at the very top of the document — a business/clinic/hospital name, logo text, address, or phone number printed above the itemized content — verbatim, even though it isn't a `label: value` field itself. Note explicitly whether the document is the institution's own pre-printed register slip (its own name printed on it, plus its own invoice/slip number, cashier ID, or similar) versus a blank generic template (checkbox/requisition form, or handwriting on the back of a drug product's own package/label) with no institution name printed on it at all — this distinction matters downstream and is easy to drop if you only transcribe the labeled fields and the stamp.
- Then, separately, note any ink stamp(s) or signature(s) visible on it, and describe each one specifically: what it says if legible (e.g. a clinic/hospital name, a pharmacy/dispensary name, a doctor's personal stamp) or "illegible stamp" if not, and roughly where it sits on the page. Do not just note "stamped" or "signed" — the specific source of the mark (clinic vs. pharmacy vs. doctor vs. unclear) matters downstream. This is in addition to, not instead of, the letterhead note above — a page can have both, or a letterhead but no legible stamp, or vice versa.
- If the page is a delegation/authorization letter — a short paragraph naming a third party being authorized to receive claim payment on the writer's behalf, followed by a block with signature, name, NRC/registration number, address, and phone number — transcribe it as `label: value` pairs for each of those signature-block fields, plus the body paragraph's authorized-party name as its own line. Do not describe or summarize the document instead of transcribing it, and do not transcribe it as if it were a claim/patient form — this document has no "Patient Name," "Claim Type," or "Treatment Date" fields; if you find yourself writing those, you are misreading the layout. If a field is illegible, write `unclear` for that specific field — do not skip straight to a general description of the page instead.

Output plain text only, not JSON. Transcribe using the label: value format described above — do not write descriptive commentary about what kind of document a page might be instead of actually transcribing it.
